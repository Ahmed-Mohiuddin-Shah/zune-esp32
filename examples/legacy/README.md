# Legacy demos (pre-refactor)

These were the PlatformIO/Arduino apps (Tetris and experiments). They are **not**
built by the CMake / ESP-IDF Stage 1 tree and still reference the old Arduino APIs.

Port them to `zyngine::Engine` + HAL when needed.
